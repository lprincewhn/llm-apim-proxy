# Live monitored failover drill — 2026-09-15

The requested asynchronous control loop succeeded in the MCAPS Developer lab.
All times below are UTC.

| Event | Time / evidence |
|---|---|
| MI GET and unchanged-value conditional PUT succeeded | Access run `08584121432615754645664044576CU06` |
| Two real backup GPT probes passed without writing | Preview run `08584121432218714293683100546CU07` |
| Five controlled `/llm/generate` requests returned 504, one attempt each | 09:15:31–09:15:36; `X-Remaining-Budget-Ms: 1` |
| Real Azure Monitor `llm-backend-errors` alert Fired | 09:17:33.0245114; alert `3d101448-5a4d-925f-aa28-529455e10004` |
| Action Group triggered the Logic App; both model probes and write succeeded | Run `08584121430318812745692113026CU01` |
| Controller changed route to Sweden, version 2 | `lastSwitchAt=2026-09-15T09:17:39.0311001Z` |
| New APIM business request returned real GPT-5.1 `OK` from Sweden | 09:17:49; HTTP 200, one attempt, client duration 2440 ms |

The drill did not manually invoke an alert callback or write the route. The
earlier preview and access checks are explicitly separate from the actual
Azure Monitor-triggered run. Action statuses confirm both real-model validation
steps, ETag-protected write and control-plane read-back succeeded. The final
business request independently confirms gateway propagation.

Approximately 2m08s elapsed from the first observed 504 to the route timestamp;
approximately 2m18s to observing a successful new Sweden request. This includes
log ingestion, alert evaluation and observation intervals; it is not a guaranteed
reaction SLA or a measured gateway-only propagation duration.

## Retained state

Both latency and error alerts remain enabled, `switchEnabled=true`. Route:

```json
{
  "primary": "sweden",
  "enabled": ["sweden"],
  "version": 2,
  "lastSwitchAt": "2026-09-15T09:17:39.0311001Z",
  "reason": "Azure Monitor llm-backend-errors quarantined eastus2"
}
```

East US 2 was quarantined because of the drill, **not because an actual Foundry
outage was observed**. There is currently no enabled backup; an operator must
validate and explicitly re-enable East US 2 for another failover. No automatic
failback or recovery was performed. Do not rerun initial configuration merely
to recover: it also resets unrelated lab state.

## Limits

The errors were real APIM business-response logs induced by tiny caller budgets.
This proves the error-alert control chain, not a naturally occurring regional
outage. Current error attribution includes such caller-budget timeouts; production
classification must distinguish caller budget exhaustion from backend health.
Latency-alert triggering was not separately exercised. Neither this smoke prompt
nor the two health probes prove sustained backup capacity, GPT quality, production
HA, Java/Search integration, or the 8s/15s business targets.

Raw sanitized observations: `drill-20260915.json`. No credentials, callback URLs,
request secrets, or embedding vectors are included.
